User-agent: *
{{- if hugo.IsProduction | or (eq site.Params.env "production") }}
Disallow:
Allow: /
# Allow access to important static resources
Allow: /css/
Allow: /js/
Allow: /img/
Allow: /images/
# Prevent crawling of search and helper pages
Disallow: /search
Disallow: /search/
{{- else }}
Disallow: /
{{- end }}

# Sitemap for discovery
Sitemap: {{ "sitemap.xml" | absURL }}

# Crawl delay (in seconds) - optional, helps with server load
Crawl-delay: 1

# Specific rules for common bots
User-agent: Googlebot
Allow: /

User-agent: Bingbot
Allow: /

User-agent: Slurp
Allow: /
